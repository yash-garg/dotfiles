#!/usr/bin/env bash

alias cls='clear'
alias gw='./gradlew'

if type bat &>/dev/null; then
    alias cat='bat'
fi

if type diskus &>/dev/null; then
    alias du='diskus'
fi

if type fastfetch &>/dev/null; then
    alias fetch='fastfetch'
fi

if type lazygit &>/dev/null; then
    alias lg='lazygit'
fi

if type git &>/dev/null; then
    alias add='git add --all'
    alias amend='git commit --amend'
    alias commit='git commit -m'
    alias fpush='git push --force-with-lease'
    alias gco='_fzf_git_each_ref --no-multi | xargs git checkout'
    alias gsw='_fzf_git_branches | sed "s|^origin/||" | xargs git switch'
    alias gswt='_fzf_git_worktrees --no-multi | xargs cd'
    alias pull='git pull'
    alias push='git push'
    alias st='git status'
fi

if type nvim &>/dev/null; then
    export EDITOR='nvim'
fi

if type micro &>/dev/null; then
    alias m='micro'
fi

if type rg &>/dev/null; then
    alias grep='rg'
fi

if type xclip &>/dev/null; then
    alias xclip='xclip -selection c'
fi

if type zoxide &>/dev/null; then
    alias cd='z'
fi

if type kubectl &>/dev/null; then
    alias k='kubectl'
fi

if type kitty &>/dev/null; then
    alias s='kitten ssh'
fi

if type batman &>/dev/null; then
    alias man='batman'
fi

if type zellij &>/dev/null; then
    alias zj='zellij'
    alias zl='zellij --layout'
    alias zr='zellij run -d right -c --'
    alias zrf='zellij run -f -c --'
    alias zri='zellij run -d right -i -c --'
    alias zkill='zellij ka && zellij da'
fi

if type just &>/dev/null; then
    alias j='just'
fi
